.env
manga/.env
manga/.source/
__pycache__/
*.pyc

# Derived, regenerable from the full-res PDF — don't duplicate 27MB in the repo
manga/volume_01/Volume01_web.pdf

# Style-reference source pages: 276MB, re-downloadable via refs/fetch_style.py
manga/refs/style/
manga/refs/style_png/

# Derived PNG cache of the style library — rebuilt on demand by refs/style_select.py
manga/refs/style_png/
lora/
# Volume 5's master is 173MB — past GitHub's 100MB per-file ceiling. Unlike earlier volumes
# it ships as Volume_05_part*.pdf (tracked) plus the compressed reader; the master is local
# only and rebuilt with `python manga/assemble_v5.py --rebuild-master`.
manga/volume_05/Volume_05.pdf

# Rejected first-pass pages kept locally for comparison; the accepted page is in raw/
manga/chapters/*/rejected_*/
